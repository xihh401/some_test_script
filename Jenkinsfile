pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'echo "aaaaaaaaa" >> /root/20200102.txt'
            }
        }
    }
}
