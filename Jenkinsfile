pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'echo "aaaaaaaaa" >> /var/lib/jenkins/20200102.txt'
            }
        }
    }
}
